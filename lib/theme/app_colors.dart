import 'package:flutter/material.dart';

class AppColors {
  // 배경/서피스
  static const Color background = Color(0xFFFAF8F5);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color border = Color(0xFFEDE9E3);

  // 텍스트
  static const Color textPrimary = Color(0xFF3D3529);
  static const Color textSecondary = Color(0xFFA89F91);

  // Primary (테라코타) — 구약, 체크 완료
  static const Color primary = Color(0xFFC47B5A);
  static const Color primaryBg = Color(0xFFFDF3EE);
  static const Color primaryBorder = Color(0xFFF2DDD2);

  // Secondary (블루그레이) — 신약
  static const Color secondary = Color(0xFF7A8E99);
  static const Color secondaryBg = Color(0xFFF2F5F7);
  static const Color secondaryBorder = Color(0xFFDDE4E9);

  // Gold (십자가, 완독)
  static const Color gold = Color(0xFFD4A843);
  static const Color goldLight = Color(0xFFF5E6C0);

  // 3D 뷰 다크 배경
  static const Color darkBg = Color(0xFF0a0a1a);

  // 3D 블록 색상
  static const Color coverBrown = Color(0xFF8B4513);
  static const Color coverDark = Color(0xFF6B3410);
  static const Color spineBrown = Color(0xFF5C2E0E);
  static const Color pageIvory = Color(0xFFFFF8E7);
  static const Color pageIvoryDark = Color(0xFFE8DFC8);
  static const Color wireframe = Color(0x33FFFFFF);
}
